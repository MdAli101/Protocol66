# Protocol66
Minimal CLI tool to navigate directories and delete files quickly. Supports folder traversal, listing entries, single file deletion, and bulk removal by extension with regex. Includes confirmation prompts and “yes to all” mode for efficient cleanup.
